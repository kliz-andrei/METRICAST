import type { RequestHandler } from 'express'; import { DashboardService, type DashboardQuery } from '../services/dashboard.service.js'; const service=new DashboardService(); const parseChannels=(value:unknown)=>typeof value==='string'?value.split(',').map(channel=>channel.trim()).filter(Boolean):undefined; const q=(request:Parameters<RequestHandler>[0]):DashboardQuery=>({startDate:typeof request.query.startDate==='string'?request.query.startDate:undefined,endDate:typeof request.query.endDate==='string'?request.query.endDate:undefined,salesChannel:typeof request.query.salesChannel==='string'?request.query.salesChannel||undefined:undefined,salesChannels:parseChannels(request.query.salesChannels),groupBy:typeof request.query.groupBy==='string'?request.query.groupBy:undefined,limit:typeof request.query.limit==='string'?Number(request.query.limit):undefined}); export const summary:RequestHandler=async(req,res)=>res.json({data:await service.summary(q(req))}); export const trend:RequestHandler=async(req,res)=>res.json({data:await service.grouped(q(req))}); export const channels:RequestHandler=async(req,res)=>res.json({data:await service.distribution('salesChannel',q(req))}); export const orderTypes:RequestHandler=async(req,res)=>res.json({data:await service.distribution('orderType',q(req))}); export const products:RequestHandler=async(req,res)=>res.json({data:await service.productRank({...q(req),limit:q(req).limit||10})}); export const categories:RequestHandler=async(req,res)=>res.json({data:await service.categories(q(req))}); export const hourly:RequestHandler=async(req,res)=>res.json({data:await service.hourly(q(req))}); export const imports:RequestHandler=async(_req,res)=>res.json({data:await service.imports()});
