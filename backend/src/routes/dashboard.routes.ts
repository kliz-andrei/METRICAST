import { Router } from 'express'; import * as dashboard from '../controllers/dashboard.controller.js'; import { asyncHandler } from '../lib/async-handler.js'; const router=Router(); router.get('/summary',asyncHandler(dashboard.summary));router.get('/sales-trend',asyncHandler(dashboard.trend));router.get('/channel-distribution',asyncHandler(dashboard.channels));router.get('/order-type-distribution',asyncHandler(dashboard.orderTypes));router.get('/top-products',asyncHandler(dashboard.products));router.get('/top-categories',asyncHandler(dashboard.categories));router.get('/hourly-sales',asyncHandler(dashboard.hourly));router.get('/recent-imports',asyncHandler(dashboard.imports));export default router;
