# Database workspace

Phase 2 will add the Prisma schema, migration workflow, source-field mapping, idempotent CSV import ledger, and seed data here.

The schema will be derived from the three Under the Balete POS exports rather than inferred from generic restaurant entities.
