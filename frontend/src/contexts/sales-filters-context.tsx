import { createContext,useContext,useState,type ReactNode } from 'react'; import type { SalesFilters } from '../services/sales-analytics.api';
const C=createContext<{filters:SalesFilters;setFilters:React.Dispatch<React.SetStateAction<SalesFilters>>}|null>(null);export function SalesFiltersProvider({children}:{children:ReactNode}){const [filters,setFilters]=useState<SalesFilters>({});return <C.Provider value={{filters,setFilters}}>{children}</C.Provider>}export const useSalesFilters=()=>{const v=useContext(C);if(!v)throw new Error('Sales filter provider is required.');return v;};
