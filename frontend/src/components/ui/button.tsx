import { forwardRef, type ButtonHTMLAttributes } from 'react'; import { cn } from '../../lib/utils';
export const Button = forwardRef<HTMLButtonElement, ButtonHTMLAttributes<HTMLButtonElement> & { variant?: 'default' | 'outline' | 'ghost' }>(({ className, variant = 'default', ...props }, ref) => <button ref={ref} className={cn('inline-flex h-10 items-center justify-center rounded-lg px-4 text-sm font-semibold transition focus:outline-none focus:ring-2 focus:ring-amber-500 disabled:opacity-50', variant === 'default' && 'bg-emerald-900 text-white hover:bg-emerald-800', variant === 'outline' && 'border border-emerald-900/20 bg-transparent text-emerald-950 dark:text-emerald-50', variant === 'ghost' && 'hover:bg-emerald-900/10', className)} {...props} />); Button.displayName = 'Button';
