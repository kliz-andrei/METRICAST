import {useQuery}from'@tanstack/react-query';import{transactionsApi,type TransactionFilters}from'../services/transactions.api';export const useTransactions=(filters:TransactionFilters)=>useQuery({queryKey:['transactions',filters],queryFn:()=>transactionsApi.list(filters),placeholderData:p=>p});export const useTransactionDetail=(id:string|null)=>useQuery({queryKey:['transaction',id],queryFn:()=>transactionsApi.detail(id as string),enabled:Boolean(id)});
